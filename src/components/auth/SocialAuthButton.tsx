import React from 'react';
import { TouchableOpacity, Text } from 'react-native';

const SocialAuthButton = () => {
  return (
    <TouchableOpacity>
      <Text>Social Auth Button</Text>
    </TouchableOpacity>
  );
};

export default SocialAuthButton;
